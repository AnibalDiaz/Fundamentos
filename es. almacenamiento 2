#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv) {
    int i=0,x=0,productos=0;
    float precioFinal,precioSuma,precioDescuento;
    float producto[x];
    
    printf("Cuantos productos son: ");
    scanf("%d",&productos);
    
    for(i;i<productos;i++){
        printf("Precio del producto %d",i+1);
        scanf("%f",producto[x]);
        
    }
    return (EXIT_SUCCESS);
}

